import { GetPositionDirective } from './get-position.directive';

describe('GetPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new GetPositionDirective();
    expect(directive).toBeTruthy();
  });
});
